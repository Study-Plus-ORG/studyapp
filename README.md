# studyapp

A new Flutter project.

start ms-settings:developers

winget install OpenJS.NodeJS.LTS && dart pub global activate flutterfire_cli && npm install -g firebase-tools && firebase login && flutterfire configure && flutter pub add firebase_core && flutter pub add cloud_firestore firebase_auth